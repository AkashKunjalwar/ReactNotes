import React from "react";

const Modal = () => {
  return (
    <div style={{ height: "100px", width: "400px", backgroundColor: "yellow" }}>
      <h1>Just a modal</h1>
      <button>Exit</button>
    </div>
  );
};

export default Modal;
